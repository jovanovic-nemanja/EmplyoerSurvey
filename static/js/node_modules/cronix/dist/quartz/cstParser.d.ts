import { BaseCstParser } from "@/common";
export declare class QuartzCstParser extends BaseCstParser {
    constructor();
    readonly cronExpression: (idxInCallingRule?: number, ...args: any[]) => import("chevrotain").CstNode;
    readonly exprNotUnion: (idxInCallingRule?: number, ...args: any[]) => import("chevrotain").CstNode;
    private readonly dow;
}
